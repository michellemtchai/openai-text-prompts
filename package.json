{
  "name": "openai-text-prompts",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "babel-loader": "^8.0.4",
    "node-fetch": "^2.0.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.3",
    "styled-components": "^5.3.5"
  },
  "scripts": {
    "prestart": "yarn --no-bin-links install",
    "start": "netlify dev --port \"$FRONTEND_PORT\" --functionsPort \"$FUNCTIONS_PORT\" --live",
    "start:app": "PORT=\"$FRONTEND_PORT\" react-scripts start",
    "build": "react-scripts build"
  },
  "devDependencies": {
    "http-proxy-middleware": "^0.19.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
