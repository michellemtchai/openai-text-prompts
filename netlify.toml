[build]
    command = "yarn build"
    functions = "src/lambda" # netlify-lambda builds to this folder AND Netlify reads functions from here
    publish = "build" # serve all these files statically

[functions]
  node_bundler = "esbuild"
